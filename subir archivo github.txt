gitpara para bajar el archivo y subirlo despues de la modificación

pasos para bajarlo:
git pull origin master
git remote add origin (link del archivo)
git pull origin master


pasos para subirlo:
git add .
git commit -m "(escribir alguna palabra)"
git push -u origin master
